# @paykernel/routing

## Unreleased

### Patch

- Docs/JSDoc: document that default `AbortError` → `not_submitted` enables multi-gateway fallback and is unsafe if abort can be post-accept; recommend `indeterminate` / explicit `submissionState` for multi-gateway.


## 0.1.0-next.0

### Major Changes

- Initial PayKernel prerelease under the `@paykernel` npm scope.
